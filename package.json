{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "PeerJS server to deploy on Heroku",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "zunkelty",
  "license": "UNLICENSED",
  "dependencies": {
    "peer": "^0.6.1"
  }
}
